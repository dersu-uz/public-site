import { bg } from './bg';
import { flex, grid, size } from './layout';
import { mq } from './media-query';
import { margin, padding } from './spacing';
import { textColor, textStyle, typography } from './typography';

const s = {
  bg,
  mq,
  margin,
  padding,
  typography,
  textStyle,
  textColor,
  size,
  grid,
  flex,
};

export default s;
